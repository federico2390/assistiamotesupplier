name: adminpanel
description: ""

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  http: ^0.13.5
  flutter_html: ^2.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^2.4.0
  firebase_messaging: ^14.1.4
  flutter_app_badger: ^1.5.0
  flutter_inappwebview: ^5.7.2+2
  flutter_local_notifications: ^12.0.4
  flutter_luban: ^0.1.13
  http_parser: ^4.0.2
  lecle_flutter_absolute_path: ^0.0.2+1
  modal_bottom_sheet: ^2.1.2
  multiple_images_picker: ^1.0.1
  package_info_plus: 
  path_provider: ^2.0.11
  photo_view: ^0.14.0
  provider: ^6.0.4
  settings_ui: ^2.0.2
  shared_preferences: ^2.0.15
  smooth_page_indicator: ^1.0.0+2
  status_alert: ^1.0.1
  uuid: ^3.0.7
  
dev_dependencies:
  build_runner: ^2.3.2
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0 # flutter packages pub run build_runner build --delete-conflicting-outputs

flutter:
  uses-material-design: true
  assets:
    - assets/